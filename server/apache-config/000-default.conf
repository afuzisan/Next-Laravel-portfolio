#    <VirtualHost *:80>
#        ServerName http://localhost

#        ProxyRequests Off
#        ProxyPreserveHost On
#        ProxyVia Full
#        <Proxy *>
#            Require all granted
#        </Proxy>

#        RewriteEngine On
#        RewriteCond %{HTTP_HOST} ^${DOCKER_RewriteCond_URL}$
#        RewriteRule ^/(.*)$ http://${DOCKER_REDIRECT_URL}/$1 [R=301,L]

#        <Location />
#            ProxyPass http://${DOCKER_REDIRECT_URL}/
#            ProxyPassReverse http://${DOCKER_REDIRECT_URL}/
#        </Location>
#    </VirtualHost>

#    <VirtualHost *:3000>
#        ServerName example.com

#        ProxyRequests Off
#        ProxyPreserveHost On
#        ProxyVia Full
#        <Proxy *>
#            Require all granted
#        </Proxy>

#        <Location />
#            ProxyPass http://localhost
#            ProxyPassReverse http://localhost:3000/
#        </Location>
#    </VirtualHost>

# <VirtualHost *:80>
#     ServerAdmin webmaster@localhost
#     DocumentRoot /var/www
#     ServerName localhost 

#     ProxyPreserveHost On
#     ProxyPass / http://client:3000/
#     ProxyPassReverse / http://client:3000/

#     ErrorLog ${APACHE_LOG_DIR}/error.log
#     CustomLog ${APACHE_LOG_DIR}/access.log combined
# </VirtualHost>